# 初心者向け改善計画

> 対象: Web開発図解サイト（https://web-dev-zukan.vercel.app）
> 対象読者: 入社したての新入社員レベルの完全初心者（プログラミング未経験〜数週間目）
> 改善方針: コード例はそのまま残す / 前提用語は逐一説明する / 論理の飛躍をなくす / 上級トピックは「応用編」に分離して順序を整える

---

## 1. 問題分析（variablesページを例に）

### 1-1. 「習っていない用語」が説明なしで突然登場する

具体的な詰まりポイント:

| 場所 | 出てくる用語 | 何が問題か |
|------|------------|----------|
| 概念図A の比較カード4行目 | 「ホイスト：undefined / TDZ」 | この時点ではホイストもTDZも未説明。意味不明のまま「TDZ」という暗号が並ぶだけ |
| 概念図C のラベル | 「parse」「JSエンジン」 | parse（構文解析）とは何か、JSエンジンとは何かが説明されていない |
| 概念図C 直後 | 「ReferenceError」 | エラーオブジェクトの概念自体を読者がまだ知らない |
| 概念図D | クロージャ全体 | この時点では「関数を返す関数」という構造を読者が読めない（高階関数を未学習） |
| 概念図E | 「スタック」「ヒープ」「アドレス」「参照」 | メモリ管理の用語が完全に未定義のまま図解されている |
| 比較表「ホイスティング」行 | 「undefined になる / TDZエラー」 | 概念図Cで初出した語を、まだ咀嚼できていないうちに表で並べて比較させている |
| DetailSection 6.1 | 「巻き上げ」「TDZ」「ReferenceError」 | ここで初めて言葉の定義が出る → 順序が逆。先に図と表で「答え」が出ているのに、定義は最後に来る |

### 1-2. 論理の飛躍

- **OnePageSummary は「箱の比喩」で柔らかい** → **直後の概念図Aで4列の特性表（「ホイスト」「TDZ」）を見せる** → 比喩から仕様表への落差が大きすぎる
- **概念図B（スコープ）→ 概念図C（ホイスティング）** の間に橋渡しがない。「なぜここで突然『コードが動く前』の話に移るのか」が読者に伝わっていない
- **概念図C → 概念図D（クロージャ）** はスコープの応用だが、初心者にとっては「変数を学びに来たのに突然『関数を返す関数』の話」になっており完全に置き去り
- **概念図E（プリミティブ vs オブジェクト）** は変数とスコープというページタイトルから外れている。「変数」のページなのにメモリ管理を扱う必然性が読者に提示されていない

### 1-3. 情報の総量と順序が読者を疲れさせる

- 概念図がA〜Fの6枚あり、そのうちD・E・Fはすべて上級トピック（クロージャ・参照型・TDZ詳細）
- 初心者が「変数とは何か」を腑に落とすために必要な情報は概念図A・Bだけで十分なのに、その後に4枚の上級図が積まれている
- MajiDialogue（対話）が一番分かりやすいのに最後の方に配置されている。読者は対話に辿り着く前に挫折する

### 1-4. 他ページにも同じ問題が波及している

ざっと他ページを見た所感:

- **async/page.tsx**: 「シングルスレッド」「イベントループ」「Promise（pending/fulfilled/rejected）」が冒頭から無説明で登場
- **dom/page.tsx**: 「ツリー構造」「オブジェクト」「イベントリスナー」が比喩と並列で出てくるが用語の定義はない
- **fetch/page.tsx**: HTTP・リクエスト・レスポンスの概念前提
- **modules/page.tsx**: import/export の前に「ファイル分割するとはどういうことか」が抜けている可能性
- **error/page.tsx**: try/catch・throw・エラーオブジェクトの基礎前提

すべて同じ構造的問題（=前提語の未説明＋上級トピックの早期投入）を持っている可能性が高い。

---

## 2. 改善原則（全ページ共通ルール）

実装者はこの章を「全ページに適用する5つのルール」として運用する。

### 原則1: 「初登場の専門語」は必ず1行で説明してから使う

**ルール**: ページ本文中で初めて専門語が出る場所に、`TermTip`（仮称）形式の小注釈を必ず付ける。

形式例（実装者向けの方針）:
- インラインの場合: `ホイスティング（=コードが動く前にJSが変数の宣言だけ先に把握しておく仕組み）`
- カード形式の場合: 概念図の前に「この図に出てくる言葉」というミニカードを置き、3〜5語を1行ずつ定義する
- 「TDZ」「ReferenceError」「parse」「JSエンジン」などはこの方式で必ずカバーする

**禁止事項**: 比較表のセル内に説明なしで専門語を入れない（例: 「ホイスト: undefined / TDZ」のような書き方は禁止）。

### 原則2: 「今このページで習うこと」と「前提知識」を明示分離する

**ルール**: 各ページの先頭、Heroの直後・OnePageSummaryの前に「このページで分かること / 前提として知っておくこと」を2列で書いた小ボックスを必ず置く。

例（variablesページ用）:
- このページで分かること: var/let/constの違い / スコープの基本 / const優先の理由
- 前提として知っておくこと: 変数とは「データに名前を付けて取っておく仕組み」 / プログラムは上から順に1行ずつ実行される / `console.log()` は中身を画面に表示する命令
- このページで触れない（=別ページで扱う）: ホイスティング・TDZ・クロージャ・参照型（応用編に分離）

このボックスがあるだけで、読者は「今は分からなくていい話」と「絶対に押さえる話」を区別できる。

### 原則3: 「基礎」と「応用」を物理的に分割する

**ルール**: 1ページの中に明示的に2つのセクション境界を作る。

```
【基礎編 — 完全初心者向け】
  Hero
  「このページで分かること / 前提知識」ボックス
  OnePageSummary（比喩）
  概念図A（最低限の図1〜2枚）
  ComparisonTable（最低限の比較）
  MajiDialogue（対話）
  ─────────────────
  「ここから先は応用編。1周目は飛ばしてOK」のセクション境界
  ─────────────────
【応用編 — 2周目以降に読む】
  概念図C・D・E・F（上級図）
  DetailSection の上級トピック
  CorrectionCard / WarningPoint
```

「飛ばしてOK」と明記されることで、初心者は読み進めることに罪悪感を持たずに済む。読み返したときに応用編へ戻ってこられる導線も確保する。

### 原則4: 「論理の飛躍」を埋めるブリッジ文を全セクション間に必ず置く

**ルール**: ConceptDiagram と ConceptDiagram の間、および基礎編→応用編の境界に、必ず1〜2行の橋渡し文を入れる。

形式例:
- 概念図A の後 → 「ここまでは『変数の3種類』の話でした。次は『その変数がどこで使えるか（=スコープ）』を見ていきます。」
- 概念図B の後 → 「スコープが理解できたら、次は『コードが動く一瞬前にJSが裏で何をしているか』を覗いてみます。これがホイスティングです。」
- 応用編の入り口 → 「ここから先は、現場で詰まったときに戻ってくるための応用知識です。最初に読むときは飛ばして、対話パートに進んで構いません。」

ブリッジ文がないと読者は「なぜ突然この話？」と感じて離脱する。

### 原則5: MajiDialogue（対話）を「最初の理解」に使う

**ルール**: MajiDialogueは現状ページ後半にあるが、これを基礎編の中（OnePageSummaryと概念図Aの間、もしくは概念図A・Bの直後）に前倒しする。

理由:
- 現状でも対話が一番分かりやすいというユーザー認識がある
- 図解の前に対話で「ざっくり何の話なのか」を体感してもらえば、図の理解速度が一気に上がる
- 上級図（クロージャ・メモリ）まで辿り着く前に、最低限の理解が完成する構造を作れる

実装案:
- 1stダイアログ（基礎編内）: var/let/constの違いとスコープの基本だけ扱う（4〜5ターン）
- 2ndダイアログ（応用編末尾）: クロージャ・const+オブジェクトの罠など上級話題を扱う（現状のMajiDialogueから抜粋）

---

## 3. variablesページの具体的な改善案

### 3-1. 新しいページ構成（変更後）

```
[基礎編]
1. Hero（変更なし）
2. ★新規: 「このページで分かること / 前提知識」ボックス
3. OnePageSummary（変更なし — ロッカーの比喩は秀逸）
4. ★新規: 「ここから図解で見ていきます」ブリッジ文
5. 概念図A: var/let/constカード（★ホイスト列を削除し3列化）
6. ★新規: ブリッジ文「次は『どこで使えるか=スコープ』」
7. 概念図B: スコープの入れ子（変更なし）
8. ★前倒し: MajiDialogue 基礎編（var/let/const + スコープのターンだけ）
9. ComparisonTable（★ホイスティング行を削除して4行に）

[応用編 セパレータ]
10. ★新規: 「ここから応用編 — 1周目は飛ばしてOK」ボックス

[応用編]
11. 概念図C: ホイスティング（★parse/JSエンジン/ReferenceErrorの語注を追加）
12. 概念図F: TDZ詳細（★概念図Cの直後に移動。連続して読めるようにする）
13. ★新規: 「ここからさらに先 — 別ページに切り出すか、独立セクションにする」境界
14. 概念図D: クロージャ（★前提として「関数を返す関数」の1段ステップ図を新設して挟む）
15. 概念図E: プリミティブ vs オブジェクト型のメモリ（★この図を残すかは要判断 — 後述）
16. MajiDialogue 応用編（const+オブジェクト・クロージャの話だけ抜粋）
17. DetailSection（章立てを基礎/応用に並び替え）
18. RelatedLinks
19. PageDrill（基礎編の問題が前半・応用編の問題が後半になるよう順序を整える）
```

### 3-2. セクション別の具体的書き換え方針

#### A. ★新規「このページで分かること / 前提知識」ボックス（実装者へ）

Heroの直後に2カラムのカードを追加する。中身の文言案:

- **このページで分かること**
  - var / let / const の3つの違い
  - 「スコープ」=変数が使える範囲、というルール
  - 迷ったら const から書き始める、という現場ルール
- **前提として知っておくこと**
  - 変数=データに名前を付けて取っておく入れ物
  - プログラムは上から下へ1行ずつ実行される
  - `console.log(x)` は x の中身を画面に表示する命令
- **このページでは深追いしないこと（応用編で扱う）**
  - ホイスティング / TDZ / クロージャ / 参照型のメモリ

→ コンポーネントとしては既存の `ConceptDiagram` の中に3カラムで作るか、`OnePageSummary` の下にもう1ブロック追加する形でよい。新規コンポーネント `Prerequisites`（仮称）を作って共通化するのが望ましい。

#### B. 概念図A（var/let/constカード）の修正

**現状**: 4行目に「ホイスト：undefined / TDZ」がある。
**変更**: この4行目を削除し、3行（再宣言・再代入・スコープ）にする。
**理由**: 基礎編の段階ではホイスト/TDZを読者は知らない。応用編で詳しく扱うので、ここでは出さない。

カード末尾の補足文も「現代のJSではこう書くのが普通」程度に弱めて、応用知識への誘導は応用編セパレータの後で行う。

#### C. 概念図C（ホイスティング）の修正

**現状**: 「parse」「JSエンジン」「ReferenceError」「TDZ」が無説明で登場。
**変更**:
1. 図の上部に小さい語注ブロックを置き、4語を1行ずつ定義する
   - JSエンジン: ブラウザに内蔵されている、JSコードを動かす本体
   - parse（パース）: コードを読んで「文法的に何が書いてあるか」を理解する作業
   - 巻き上げ（ホイスティング）: コードが動く前に、JSエンジンが「変数や関数の宣言」だけ先に把握しておく仕組み
   - ReferenceError: 「まだ準備できていない変数を使おうとした」ときに出るエラーの種類
2. 図の右側のカード「var → undefined」「let/const → TDZ」は残すが、TDZには注釈を付ける。「TDZ=触ろうとするとエラーになる『立ち入り禁止』の期間」

#### D. 概念図F（TDZ）を概念図Cの直後に移動

**現状**: 概念図F は概念図E の後ろにある。
**変更**: 概念図F を概念図C の直後に移動する。
**理由**: ホイスティングとTDZは同じ話の表裏。連続して読ませるべき。

#### E. 概念図D（クロージャ）の前にステップ図を挟む

**現状**: いきなり「外側の関数が終わっても内側の関数が変数を覚えている」という結論図が出る。
**変更**: 概念図D の前に、3ステップの前提図を新設する。
1. ステップ1「関数の中で変数を作れる」(`function f() { let x = 1; }`)
2. ステップ2「関数の中で関数を作れる」(`function f() { function g() {} }`)
3. ステップ3「関数は値として返せる」(`function f() { return function g() {}; }`)
これを踏んでから概念図D（=ステップ4: 返した関数が外側の変数を覚えている）に進む。

#### F. 概念図E（プリミティブ vs オブジェクト）の判断

**判断**: このページのスコープから外し、別ページ「型とメモリ」（仮）として切り出すのが理想。
**現実解（短期対応）**: 切り出しが難しい場合、応用編末尾に置き、図の冒頭に「これは厳密には『変数』というより『データの型』の話。1周目は飛ばしてOK」と明記する。

#### G. ComparisonTable の修正

**現状**: ホイスティング行が含まれており、初心者がここで詰まる。
**変更**: 基礎編にあるComparisonTable では「スコープ・再宣言・再代入・推奨度」の4行に削減。「ホイスティング」行は応用編内に独立した小さな比較表として再掲する。

#### H. DetailSection の章立て順序

**現状**: 6.1 ホイスティング → 6.2 クロージャ → 6.3 const+オブジェクト → 6.4 実践ルール
**変更**: 順序を「6.1 実践ルール（const ファースト）→ 6.2 const+オブジェクト → 6.3 ホイスティング → 6.4 クロージャ」に並び替える。
**理由**: 初心者にとって最も実用的な「実践ルール」を先頭に置く。仕様の細部は後ろへ。

---

## 4. 用語解説の追加案

「これらは全ページ共通の前提語」として、共通用語集として扱う。各ページで初出時にインライン注釈または `Prerequisites` ボックスに含める。

### 4-1. JS全般の前提語（最優先で説明すべき10語）

| 用語 | 1行説明（実装者向けの文言案） |
|------|------------------------------|
| 変数 | データに名前を付けて取っておくための入れ物。あとで名前で呼び出せる |
| 値 | 変数に入れる中身そのもの（数字・文字列・true/false など） |
| 宣言 | 「これから〇〇という名前の変数を使います」とJSに教える行（`let x` の部分） |
| 代入 | 変数に値を入れる操作（`x = 10` の `=` のこと） |
| 関数 | ひとまとまりの処理に名前を付けたもの。呼び出すと中の処理が動く |
| 引数 | 関数に渡す材料。`f(x)` の `x` の部分 |
| 戻り値 | 関数が処理の結果として返す値 |
| 文字列 / 数値 / 真偽値 | データの種類。それぞれ "abc" / 42 / true,false |
| エラー | コードが動かなかったときにJSが投げてくる「困った」のお知らせ |
| `console.log()` | カッコの中身を画面（開発者ツールのコンソール）に表示する命令 |

### 4-2. variablesページ固有の前提語

| 用語 | 1行説明 |
|------|---------|
| スコープ | 変数が使える範囲のルール。「この変数はここからここまでだけ使えます」という線引き |
| ブロック | `{ }` で囲まれた範囲のこと。if文・for文・関数の本体などがブロック |
| ホイスティング | コードが動く前に、JSが「変数や関数の宣言」だけ先に把握しておく仕組み |
| TDZ | Temporal Dead Zone の略。let/const を宣言した行よりも前に変数を触ろうとするとエラーになる、という「立ち入り禁止期間」 |
| ReferenceError | 「まだ準備できていない変数を使おうとした」ときにJSが出すエラーの種類 |
| JSエンジン | ブラウザに内蔵されている、JSコードを動かす本体（V8など） |
| parse / 構文解析 | コードを読んで「文法的に何が書いてあるか」を理解する作業 |
| クロージャ | 関数の外で宣言された変数を、その関数が「覚えていて、後からも使える」性質 |
| プリミティブ型 | 値そのものを直接持つ単純なデータ型（数値・文字列・真偽値など） |
| オブジェクト型 | データのまとまり（キーと値のペア）を持つ複雑なデータ型 |
| 参照 | 「データそのもの」ではなく「データがどこにあるかの住所」だけを持っている状態 |

### 4-3. 他ページの前提語（参考リスト）

実装時に各ページで初出する語を確認し、対応するボックス・注釈を追加する。

- **async/page.tsx**: シングルスレッド / 同期 / 非同期 / Promise / pending,fulfilled,rejected / コールバック / イベントループ / マイクロタスク
- **dom/page.tsx**: HTML / ツリー構造 / ノード / 要素 / 属性 / イベント / イベントリスナー / バブリング
- **fetch/page.tsx**: HTTP / リクエスト / レスポンス / メソッド（GET/POST など） / ステータスコード / JSON / API / CORS
- **modules/page.tsx**: ファイル / モジュール / import / export / 名前空間 / デフォルトエクスポート
- **error/page.tsx**: 例外 / try / catch / throw / エラーオブジェクト / スタックトレース

各ページに対応する `Prerequisites`（=「このページで分かること / 前提知識 / 触れないこと」ボックス）を追加することが必須。

### 4-4. 実装案: 共通コンポーネント `Prerequisites`

実装者は次の3点を持つコンポーネントを新設するのが望ましい:

1. props: `learn`（このページで分かることの配列）、`prerequisites`（前提知識の配列）、`outOfScope`（このページで深追いしないことの配列）
2. デザイン: Hero直下に置けるサイズ感。3カラムカードまたは縦積みカード
3. 配置: 全ページ共通でHero と OnePageSummary の間に挿入する

---

## 5. 他ページへの適用指針

variablesページで確立した5原則をそのまま他のJSページに横展開する。各ページ固有の注意点を以下にまとめる。

### 5-1. async/page.tsx（非同期処理）

**特に注意すべき飛躍**:
- 「シングルスレッド」を理解する前に「イベントループ」が出てくる順序は禁止
- 「Promiseの3状態（pending/fulfilled/rejected）」を比喩で導入する前に状態名を出さない
- async/await はPromiseの理解前提なので、Promiseを完全に説明し終わってから登場させる

**応用編に分離すべき**:
- マイクロタスクキュー / マクロタスクキューの細かい話
- Promise.all / Promise.race / Promise.allSettled の比較
- async関数の戻り値が常にPromiseになる仕組み

**Prerequisites案**:
- 前提: 関数の基礎 / `setTimeout` を見たことがあること / コールバックという概念（=関数を関数に渡せる）

### 5-2. dom/page.tsx（DOM操作）

**特に注意すべき飛躍**:
- 「DOM = ツリー構造」と最初に言うが、ツリー構造を初心者は知らない可能性がある → 家系図の比喩で説明する
- `querySelector` の引数（CSSセレクタ）は、CSS未学習者にとってはそれ自体が前提
- イベント伝播（バブリング・キャプチャ）は基礎編に入れない

**応用編に分離すべき**:
- innerHTML の XSSリスク
- 仮想DOM との対比
- カスタムイベント

**Prerequisites案**:
- 前提: HTMLが「タグの入れ子構造」であること / CSSセレクタ（`.class`, `#id`）の基本

### 5-3. fetch/page.tsx（fetch API）

**特に注意すべき飛躍**:
- HTTPメソッド・ステータスコード・JSON はそれぞれ独立した前提知識
- `Response` オブジェクトと `.json()` の挙動はPromise未理解だと意味不明
- CORSは応用編で十分

**応用編に分離すべき**:
- CORS / preflight
- AbortController によるキャンセル
- fetch のエラー判定（ネットワークエラー vs HTTPエラー）の話

**Prerequisites案**:
- 前提: Promise（=async/awaitの話）を先に読んでいること / HTTP=Webページのデータをやり取りする決まり / JSON=データを文字列で表す共通フォーマット

### 5-4. modules/page.tsx（ESモジュール）

**特に注意すべき飛躍**:
- 「ファイルを分ける」という発想自体が初心者には飛躍。なぜ分けるのかのモチベーションを必ず先に書く
- `default export` と `named export` の違いは2回目以降で十分
- Tree shaking などのバンドル時最適化の話は応用編

**応用編に分離すべき**:
- 動的import
- CommonJSとの違い

**Prerequisites案**:
- 前提: 1ファイルにコードを全部書くと長くなって困る、という実感 / 関数を別ファイルから呼ぶと便利、という想像

### 5-5. error/page.tsx（エラーハンドリング）

**特に注意すべき飛躍**:
- 「エラーをcatchする」と言われても、エラーが「投げられている」ことが分からないと catch が何をしているか分からない
- `throw` は基礎編には入れにくい。基礎編は try/catch に絞る
- 非同期エラー（Promise.catch / async関数内の try）は応用編

**応用編に分離すべき**:
- カスタムエラークラス
- エラーチェイン
- 非同期エラー処理の細部

**Prerequisites案**:
- 前提: エラーが発生するとプログラムが止まる、という体験 / 関数の呼び出しが入れ子になっている、というイメージ

### 5-6. 全ページ共通の実装ルール（再掲）

1. Heroの直下に `Prerequisites`（このページで分かること/前提/触れないこと）を必ず置く
2. ConceptDiagram の中に「初登場語」がある場合、その図の冒頭に語注ブロックを必ず置く
3. ConceptDiagram と ConceptDiagram の間に1〜2行のブリッジ文を入れる
4. 「ここから応用編」のセパレータコンポーネントを基礎編末尾に必ず置く
5. MajiDialogue は基礎編・応用編に2分割し、基礎編の対話を上の方に前倒しする
6. ComparisonTable のセル内に未説明の専門語を入れない
7. DetailSection の章順は「実践ルール → よくある誤解 → 仕様の詳細 → 上級トピック」とする

---

## 6. 実装の進め方（提案）

### Phase 1: 共通コンポーネント整備（1日）
- `Prerequisites` コンポーネント新設（learn / prerequisites / outOfScope の3カラム）
- `SectionDivider` コンポーネント新設（「ここから応用編」用のセパレータ）
- `TermTip` または `TermNote` コンポーネント新設（語注のインライン表示用）

### Phase 2: variablesページを完全リニューアル（1日）
- 上記の改善案をすべて適用
- ユーザーレビューを受けて初心者目線で読めるか確認

### Phase 3: 他5ページへの横展開（各0.5日 × 5）
- async → dom → fetch → modules → error の順
- 各ページに `Prerequisites` を追加し、応用編セパレータを挿入
- 概念図の語注を追加し、論理の飛躍を埋める

### Phase 4: 全ページ通読チェック（0.5日）
- 「入社1日目の自分が読めるか」のチェックリストで全ページを通読
- PageDrillの問題が基礎編→応用編の順になっているか確認

---

## 7. 完了の判定基準

各ページについて、以下のチェックがすべて埋まったら完了とする。

```
□ Hero直下に Prerequisites ボックスがある
□ 「このページで触れないこと」が明示されている
□ 概念図の中に未説明の専門語が1つもない
□ 概念図と概念図の間に橋渡し文がある
□ 「ここから応用編」のセパレータがある
□ MajiDialogue 基礎編が ComparisonTable より前にある
□ ComparisonTable のセルに未説明語がない
□ DetailSection が「実践 → 誤解 → 詳細 → 上級」の順
□ PageDrill の問題が基礎→応用の順に並んでいる
□ 「入社1日目の新入社員」を想定した通読で詰まる箇所がない
```
