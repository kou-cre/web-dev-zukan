# Web開発図解サイト カリキュラム設計書

> 対象：完全初心者（入社1日目の新入社員レベル／プログラミング未経験〜数週間目）
> 前提：`初心者向け改善計画.md` で確立した5原則（前提語の説明・基礎/応用の分離・橋渡し文・対話の前倒し・触れないことの明示）を全カリキュラムに横展開する
> スタック：Next.js 15（App Router）+ TypeScript + Tailwind CSS + lucide-react（変更なし）
> 作成日：2026-05-06

---

## 1. JavaScript カリキュラム

### 1-1. 学習順序と依存関係

#### 結論：現状の6ページを8ページに再構成（+intro新設+非同期を2分割）

**依存関係マップ（A は B を理解していないと意味不明）:**

```
[intro] JavaScriptを始める前に（共通前提ページ・新規）
    ↓
[1] 変数とスコープ（基礎編のみ）
    ↓
[2] DOM操作（基礎編のみ：取得・書き換え・クリックイベント）
    ↓
[3] 同期と非同期の違い（基礎編のみ・async分割①）
    ↓
[4] Promise と async/await（async分割②）
    ↓
[5] fetch API（[4]が終わってから）
    ↓
[6] エラーハンドリング（try/catch だけ。同期→非同期の順）
    ↓
[7] ESモジュール（最後。他ページのコード例を「複数ファイルに分けるなら」と振り返る）
```

#### 学習順序を変えた理由

| 旧順序 | 新順序 | 変えた理由 |
|--------|--------|-----------|
| 1. 変数とスコープ | intro. はじめに読む（新規） | JS前提（console.log・ブラウザ・JSエンジン）を1ページに集約 |
| 2. 非同期処理 | 1. 変数とスコープ | 不変。すべての出発点 |
| 3. DOM操作 | 2. DOM操作 | 「画面が変わる体験」で学習動機を先に与える |
| 4. fetch API | 3. 同期と非同期の違い（**分割①**） | 概念だけを先に。setTimeoutで体験させる |
| 5. ESモジュール | 4. Promise と async/await（**分割②**） | fetchの手前で完全消化させる |
| 6. エラーハンドリング | 5. fetch API | Promise直後に応用として置く |
| | 6. エラーハンドリング | fetchの後ろ=「fetch失敗をcatchする」文脈が自然 |
| | 7. ESモジュール | 「ここまでのコードをファイル分割するなら」として締める |

#### JavaScript カテゴリ最終構成（合計8ページ）

```
/javascript                      ハブ
/javascript/intro                はじめに読む（新規・共通前提）
/javascript/variables            変数とスコープ
/javascript/dom                  DOM操作
/javascript/async-basics         同期と非同期の違い（新規・分割①）
/javascript/promise              Promise と async/await（新規・分割②）
/javascript/fetch                fetch API
/javascript/error                エラーハンドリング
/javascript/modules              ESモジュール
```

### 1-2. 各ページの基礎/応用の境界

#### intro：JavaScriptを始める前に

| 区分 | 内容 |
|------|------|
| **基礎編** | プログラムとは何か（1行ずつ上から実行される）／JSとはWebの動きを担う言語（HTML・CSS・JSの3点比較）／ブラウザとJSエンジンの最小図解／DevToolsの開き方（スクショ）／console.logとは何か／エラーが出たらConsoleの最初の行を読む |
| **扱わない** | 変数・関数・型・TypeScript・Node.js・歴史 |

#### 変数とスコープ

| 区分 | 内容 |
|------|------|
| **基礎編** | var/let/constの3種類（再宣言・再代入・スコープの3列のみ）／スコープ=「使える範囲」の概念図／constファーストの現場ルール |
| **応用編** | ホイスティング／TDZ（Temporal Dead Zone）／TDZと関連する概念図 |
| **扱わない** | クロージャ（別ページ化候補）／プリミティブvsオブジェクト型のメモリ（別ページ化候補） |

#### DOM操作

| 区分 | 内容 |
|------|------|
| **基礎編** | DOM=「HTMLの家系図」の比喩／querySelector・textContent・addEventListener('click')の3点セット／ボタンを押すと文字が変わる最小例 |
| **応用編** | innerHTML・XSSリスク／createElement+appendChild／イベントオブジェクト |
| **扱わない** | イベント伝播（バブリング・キャプチャ）／仮想DOM |

#### 同期と非同期の違い（async分割①）

| 区分 | 内容 |
|------|------|
| **基礎編** | 「JSは1人だけのレジ係」=シングルスレッドの比喩／setTimeoutで「あとで実行」を体験／コールバック=関数を関数に渡せる |
| **応用編** | コールバック地獄の問題提起→次ページへ橋渡し／イベントループの簡易図解 |
| **扱わない** | Promise・async/await（次ページ）／マイクロタスク・タスク優先度 |

#### Promise と async/await（async分割②）

| 区分 | 内容 |
|------|------|
| **基礎編** | Promise=「あとで結果が届く約束のチケット」の比喩／3状態（pending/fulfilled/rejected）を信号機で説明／async/await=「.thenをきれいに書き直したもの」 |
| **応用編** | Promise.all／Promise.race／async関数の戻り値が常にPromiseになる仕組み |
| **扱わない** | fetch（次ページ）／非同期エラー処理の詳細（エラーページ） |

#### fetch API

| 区分 | 内容 |
|------|------|
| **基礎編** | HTTP=「Webのデータやり取りの決まり」／GET=「ください」/POST=「送ります」のみ／JSON=「データを文字列にする共通フォーマット」／fetch+async/awaitの最小GET例 |
| **応用編** | POST+body+headers／ステータスコード／response.okの判定 |
| **扱わない** | CORS・preflight／AbortController／axios等のライブラリ |

#### エラーハンドリング

| 区分 | 内容 |
|------|------|
| **基礎編** | エラー=「コードが動けなくなって投げるSOS」／try/catch=「エラーを受け止めるネット」／同期エラーの最小例（JSON.parseの失敗） |
| **応用編** | throwで自分からエラーを出す／finally／非同期エラー（async関数内try/catch） |
| **扱わない** | カスタムエラークラス／エラーチェイン／Sentryなど監視ツール |

#### ESモジュール

| 区分 | 内容 |
|------|------|
| **基礎編** | 「1ファイルに全部書くと長くて困る」という動機／export+importの最小例（named export1個）／`./`で同じフォルダを指す |
| **応用編** | default exportとnamed exportの違い／rename import |
| **扱わない** | 動的import／CommonJSとの違い／Tree shaking／バンドラ（webpack/Vite） |

### 1-3. 共通前提知識ページ（/javascript/intro）

必要。JavaScriptハブ直下に「はじめに読む」1ページを新設する。

**内容**:
1. プログラムとは（1行ずつ上から実行される）
2. JavaScriptとは（HTML・CSS・JSの3点比較図）
3. ブラウザとJSエンジン（V8の最小図）
4. DevToolsの開き方（右クリック→検証→Console）
5. `console.log()` とは（3行サンプルのみ）
6. エラーが出たらConsoleの最初の行を読む

---

## 2. 初心者向けページテンプレート（全カテゴリ共通）

### 2-1. 標準セクション構成

```
─────────────── 【基礎編 — 完全初心者向け】 ───────────────
①   Hero
②   Prerequisites（新規）：このページで分かること / 前提知識（他ページへのリンク付き） / このページで触れないこと
③   OnePageSummary（比喩・一言定義）
④   Bridge（橋渡し文・2行以内）
⑤   ConceptDiagram（基礎図：最大2枚）
⑥   Bridge（図と図の間）
⑦   MajiDialogue 基礎編（★ComparisonTableより前に前倒し）
⑧   ComparisonTable（基礎の列のみ／未説明語ゼロ）
⑨   PageDrill 基礎編（4問・易）
─────────────── 【SectionDivider「ここから応用編 — 1周目は飛ばしてOK」】 ───────────────
⑩   ConceptDiagram（応用図：最大4枚）
⑪   Bridge（応用図間）
⑫   MajiDialogue 応用編
⑬   DetailSection（実践ルール→よくある誤解→仕様詳細→上級トピック の順）
⑭   PageDrill 応用編（4問・難）
⑮   RelatedLinks（「前提として読むべきページ」と「次に読むべきページ」の2グループ）
```

### 2-2. 追加が必要な新規コンポーネント

| コンポーネント | 役割 | 必須化 |
|--------------|------|-------|
| `Prerequisites` | Hero直下「分かること/前提/触れないこと」3カラムボックス | 全ページ必須 |
| `SectionDivider` | 「ここから応用編 — 1周目は飛ばしてOK」帯 | 基礎編末尾に必須 |
| `Bridge` | セクション間の橋渡し文（2行以内） | 全ConceptDiagram間に必須 |
| `TermNote` | 図冒頭に置く「この図で出てくる言葉」語注ブロック | 初出語がある図に必須 |

### 2-3. ConceptDiagram の枚数ルール

| 区分 | 上限 | 備考 |
|------|------|------|
| 基礎編 | **2枚** | 1枚目=正体は何か・2枚目=どう動くか/使い分けるか |
| 応用編 | 4枚 | 合計6枚を超えたらページ分割を検討 |
| ページ合計 | **6枚** | 6枚超えは設計ミス |

### 2-4. Bridge（橋渡し文）の必須箇所

1. OnePageSummary → 最初のConceptDiagram の間
2. ConceptDiagram と ConceptDiagram の間（全箇所）
3. 最後のConceptDiagram → MajiDialogue の間
4. MajiDialogue → ComparisonTable の間
5. 基礎編末尾 → SectionDivider の手前

**Bridge文体ルール**: 2行以内・「ここまで〇〇、次は〇〇」の対構造・専門語を使わない

---

## 3. React カリキュラム

### 3-1. 学習順序と依存関係

```
[intro] ReactをはじめるためのJSチェックリスト（新規）
    ↓
[1] コンポーネント
    ↓
[2] Props と State（ペアで1ページ）
    ↓
[3] useState
    ↓
[4] useEffect
    ↓
[5] useContext（Prop drilling問題の解決として）
    ↓
[6] ルーティング（react-router）
```

#### React カテゴリ最終構成（合計7ページ）

```
/react                  ハブ
/react/intro            ReactをはじめるためのJSチェックリスト（新規）
/react/component        コンポーネント
/react/props-state      Props と State
/react/use-state        useState
/react/use-effect       useEffect
/react/use-context      useContext
/react/routing          ルーティング（react-router）
```

### 3-2. 各ページの基礎/応用の境界

#### コンポーネント

| 区分 | 内容 |
|------|------|
| **基礎編** | コンポーネント=「画面の部品」（レゴブロックの比喩）／関数コンポーネントの最小例／別ファイルへのimport1パターン／JSXの「HTMLっぽいけどJSの中に書ける」 |
| **応用編** | クラスコンポーネント（歴史紹介のみ）／フラグメント（`<>...</>`） |
| **扱わない** | コンポーネント設計論・Atomic Design |

#### Props と State

| 区分 | 内容 |
|------|------|
| **基礎編** | Props=「親から渡される変えられない手紙」/ State=「自分の中で変化する記憶」の対比図／親→子Props渡しの最小例 |
| **応用編** | children prop／Prop drilling問題提起 → useContextへの橋渡し |
| **扱わない** | Redux・useReducer |

#### useState

| 区分 | 内容 |
|------|------|
| **基礎編** | `const [count, setCount] = useState(0)` の構文／カウンターの最小例／なぜ `count = count + 1` ではダメか |
| **応用編** | 関数アップデート形式 `setCount(c => c + 1)`／オブジェクトStateの更新 |
| **扱わない** | useReducer・状態管理ライブラリ |

#### useEffect

| 区分 | 内容 |
|------|------|
| **基礎編** | 「コンポーネントが画面に出た後にやりたいこと」の比喩／空配列`[]`=1回だけ実行／依存配列に変数を入れる=変わったら実行 |
| **応用編** | 依存配列の罠（古いstateを参照）／クリーンアップ（タイマー止めるパターン） |
| **扱わない** | StrictModeの二重実行 |

#### useContext

| 区分 | 内容 |
|------|------|
| **基礎編** | Prop drilling問題の図解／createContext+Provider+useContextの3点セット最小例 |
| **応用編** | Context分割で再レンダリング最適化／useReducer+Contextパターン |
| **扱わない** | Redux・Zustand・Jotai |

#### ルーティング（react-router）

| 区分 | 内容 |
|------|------|
| **基礎編** | 「URLが変わると見せるコンポーネントが変わる」のメンタルモデル／BrowserRouter+Routes+Routeの最小例／LinkでのURL遷移 |
| **応用編** | URLパラメータ`:id`・useNavigate・ネストされたルート |
| **扱わない** | Next.jsのルーティング→Next.jsカテゴリへ |

### 3-3. JS → React の前提知識チェックリスト

React 学習前に以下が必須。`/react/intro` でチェックリスト化する:

```
□ const / let の使い分け
□ 関数（function宣言・アロー関数）
□ オブジェクトと配列の基本
□ 配列の map
□ 分割代入（const { x } = obj / const [a, b] = arr）
□ スプレッド構文（{ ...obj } / [...arr]）
□ 三項演算子（cond ? a : b）
□ ESモジュール（import / export）
□ DOM（querySelector・addEventListener）の基本
□ async/await の最小理解（useEffect内でfetchするため）
```

---

## 4. Next.js カリキュラム

### 4-1. 学習順序と依存関係

```
[intro] Next.jsをはじめる前のチェックリスト（新規）
    ↓
[1] App Router と Pages Router（Next.jsとは何かの入口を兼ねる）
    ↓
[2] データフェッチ（Server Componentsの手前に置く）
    ↓
[3] Server Components（[2]のfetchが「サーバーで動くこと」として種明かし）
    ↓
[4] API Routes（自分でエンドポイントを作る）
```

**なぜデータフェッチをServer Componentsより前にするか**: Server Componentsは「何が嬉しいか」が見えにくい。まずfetchを体験させて「awaitがそのまま書けて、クライアントに何も渡らない」現象を見せた後、「これを実現しているのがServer Componentsです」と種明かしする方が腑に落ちる。

#### Next.js カテゴリ最終構成（合計5ページ）

```
/nextjs                  ハブ
/nextjs/intro            Next.jsをはじめる前のチェックリスト（新規）
/nextjs/routing          App Router と Pages Router
/nextjs/data-fetching    データフェッチ
/nextjs/server-component Server Components
/nextjs/api-routes       API Routes
```

### 4-2. 各ページの基礎/応用の境界

#### App Router と Pages Router

| 区分 | 内容 |
|------|------|
| **基礎編** | 「ファイルを置く=ルートになる」ファイルベースルーティングの比喩／app/page.tsxの最小例／「Next.jsはReactにルーティングとサーバー機能を足したフレームワーク」の位置づけ／App Router一択であることの明記 |
| **応用編** | layout.tsxの入れ子・loading.tsx・error.tsx等のファイル規約・Route Groups |
| **扱わない** | next.config.tsの細部・i18n routing・Middleware |

#### データフェッチ

| 区分 | 内容 |
|------|------|
| **基礎編** | Server Componentで直接 `const data = await fetch(...)` を書ける現象／useEffect+fetchとの比較（旧Reactと新Next.jsの対比）／JSONを表示するだけの最小例 |
| **応用編** | fetchのキャッシュオプション（cache: 'force-cache' / 'no-store'）／revalidate |
| **扱わない** | Server Actions・streaming・suspense |

#### Server Components

| 区分 | 内容 |
|------|------|
| **基礎編** | 「Reactのコンポーネントがサーバーで動く」とはどういうことか／Server vs Client ComponentのでんpurposesターTable／'use client'ディレクティブ／useState・useEffectはClient側だけ |
| **応用編** | Server→Clientへのprops（シリアライズ可能な値のみ）／Client内にServerをchildrenで差し込むパターン |
| **扱わない** | Streaming SSRの内部構造・RSC payload詳細 |

#### API Routes

| 区分 | 内容 |
|------|------|
| **基礎編** | `app/api/.../route.ts` でGETハンドラを作る最小例／Response.json()で返す／クライアントから fetch('/api/...') で叩く |
| **応用編** | POSTハンドラ／URLパラメータ・クエリ・環境変数の使い方 |
| **扱わない** | Server Actionsとの使い分け・認証ミドルウェア |

### 4-3. React → Next.js の前提知識チェックリスト

```
□ コンポーネントを定義してimport/exportできる
□ Propsを渡せる
□ useStateで値を持って画面を更新できる
□ useEffectでfetchを呼ぶパターンが書ける
□ mapで配列をレンダリングできる
□ async/awaitでfetchが書ける
□ JSONを扱える
□ サーバーとクライアントの違いを理解している（/kiso/server を読了）
□ HTTPメソッド（GET/POST）の基本を知っている
```

---

## 5. 実装フェーズ計画

| フェーズ | 内容 | 成果物 |
|---------|------|--------|
| **Phase 0** | 共通コンポーネント整備 | Prerequisites / SectionDivider / Bridge / TermNote を新設 |
| **Phase 1** | JS既存ページの再構成 | intro新設→variables改修→dom→async分割（async-basics+promise）→fetch→error→modules |
| **Phase 2** | Reactカテゴリ新設 | intro→component→props-state→use-state→use-effect→use-context→routing |
| **Phase 3** | Next.jsカテゴリ新設 | intro→routing→data-fetching→server-component→api-routes |
| **Phase 4** | 全ページQA | 初心者向け改善計画.md §7 チェックリストで全ページ通読 |
